{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "lib" /* Specify an output folder for all emitted files. */
  },
  "include": ["src/**/*.js", "src/**/*.ts", "tsfixme.d.ts"],
  "exclude": ["tests/**"]
}
