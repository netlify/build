{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "lib" /* Specify an output folder for all emitted files. */
  },
  "include": ["src/**/*.js", "src/**/*.ts"],
  "exclude": ["tests/**"]
}
