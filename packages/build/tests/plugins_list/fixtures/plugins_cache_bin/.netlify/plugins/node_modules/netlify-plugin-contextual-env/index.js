import { execa  } from 'execa'

export const onPreBuild = async function () {
  try {
    await execa('netlify-config', ['--version'], { stdio:'inherit' })
  } catch(err) {
    // This helps debugging the error in the snapshot if something is failing
    console.log(err)
  }
}
