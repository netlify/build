import { createRequire } from 'module'

import mod1 from '@org/yet-another-test';

const require = createRequire(import.meta.url)

const filePath = require.resolve(`./files/${name}.json`)

export default async arg => (await import(filePath)) + mod1(arg);
